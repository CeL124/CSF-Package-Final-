from distutils.core import setup

setup(
    name='svhndata',
    version='1.0',
    packages=['svhndata'],
    description = ("Package will download, load, and format SVHN dataset for machine learning modeling")
)
